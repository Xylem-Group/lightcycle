//! TRON P2P client. Speaks the protobuf-over-TCP protocol used by `java-tron`.
//!
//! Stub. Per SCAFFOLD.md priority 3. Lands once we want raw protobuf
//! blocks (not JSON via the HTTP RPC) — currently the RPC head poller
//! covers the comparison-dashboard surface area.
